<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movesmart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name=".MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="movesmart"
        android:enableOnBackInvokedCallback="true">

        <!-- 카카오 SDK의 인증 활동 추가 -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme='kakao@string/kakao_app_key' android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- 네이버 로그인 Activity 추가 -->
        <activity
            android:name="com.naver.sdk.auth.NidOAuthBridgeActivity"
            android:exported="true">
            <intent-filter android:label="NaverLogin">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="movesmart" android:host="auth" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.movesmart.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 네이버 로그인 관련 설정 추가 -->
        <meta-data
            android:name="com.naver.sdk.client_id"
            android:value="@string/client_id" />  <!-- 네이버에서 발급받은 Client ID -->
        <meta-data
            android:name="com.naver.sdk.client_secret"
            android:value="@string/client_secret" />  <!-- 네이버에서 발급받은 Client Secret -->
        <meta-data
            android:name="com.naver.sdk.clientName"
            android:value="@string/client_name" />

        <!-- 카카오 앱 키 추가 -->
        <meta-data
            android:name="kakao_app_key"
            android:value="@string/kakao_app_key"/>

        <!-- Google Maps API 키 추가 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/com.google.android.geo.API_KEY"/> <!-- Google Maps API 키 -->

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
